# ============================================
# Application Settings
# ============================================
APP_NAME="Ovi Pregnancy Nutrition API"
DEBUG=True
VERSION="0.1.0"
API_PREFIX="/api"

# ============================================
# Security (REQUIRED in prod)
# ============================================
SECRET_KEY="your-secret-key-here"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days

# ============================================
# Database (REQUIRED)
# ============================================
DATABASE_URL="postgresql://user:password@localhost:5432/ovi"

# ============================================
# Supabase Auth (REQUIRED — Supabase is the canonical auth provider)
# ============================================
SUPABASE_URL="your-supabase-url"
SUPABASE_KEY="your-supabase-key"  # anon public key
# JWT verification for Supabase Auth tokens.
# Primary path: HS256 shared secret. Get it from Supabase Dashboard →
# Settings → API → JWT Settings → JWT Secret.
SUPABASE_JWT_SECRET="your-supabase-jwt-secret"
# Optional JWKS / issuer overrides (defaults derived from SUPABASE_URL).
SUPABASE_JWKS_URL="https://YOUR_PROJECT_REF.supabase.co/auth/v1/keys"
# SUPABASE_JWT_ISSUER="https://YOUR_PROJECT_REF.supabase.co/auth/v1"
SUPABASE_JWT_AUDIENCE="authenticated"

# Legacy email/password login. Disabled by default. Set to true ONLY for dev.
LEGACY_AUTH_ENABLED=false

# ============================================
# Redis (REQUIRED for rate limiting + async jobs)
# ============================================
REDIS_URL="redis://localhost:6379/0"
RATE_LIMIT_BACKEND="redis"  # redis | memory
RATE_LIMIT_REDIS_URL="redis://localhost:6379/0"

# ============================================
# External APIs (REQUIRED)
# ============================================
USDA_API_KEY="your-usda-api-key"
SPOONACULAR_API_KEY="your-spoonacular-api-key"
GEMINI_API_KEY="your-gemini-api-key"  # Get from https://ai.google.dev/

# ============================================
# Email (OPTIONAL — only required if LEGACY_AUTH_ENABLED=true and password reset is used)
# ============================================
SMTP_SERVER="smtp.gmail.com"
SMTP_PORT=587
SMTP_USERNAME="your-email@gmail.com"
SMTP_PASSWORD="your-email-password"
EMAIL_FROM="your-email@gmail.com"
EMAIL_RESET_TOKEN_EXPIRE_HOURS=24

# ============================================
# CORS
# ============================================
CORS_ORIGINS="http://localhost:3000,http://localhost:19006,exp://*"

# ============================================
# Observability (OPTIONAL)
# ============================================
# OTEL_ENABLED=false
# OTEL_EXPORTER_OTLP_ENDPOINT="http://localhost:4318"
