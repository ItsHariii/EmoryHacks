// Tab bar measurements re-exported for screen padding calculations.
export const TAB_BAR_HEIGHT = 70;
export const TAB_BAR_BOTTOM_MARGIN = 20;
// Total clearance a screen's scroll content / fixed CTA needs above the tab bar
// (callers add useSafeAreaInsets().bottom to this).
export const TAB_BAR_CLEARANCE = TAB_BAR_HEIGHT + TAB_BAR_BOTTOM_MARGIN; // 90
